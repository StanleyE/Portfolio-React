import React, { Component } from 'react';

class ProjectBoard extends Component {
    render() {
        return (
            <div>
                <h4>Chromological and interactive grid like display of past and current work; hover will have a slide and haze function that will give more details</h4>
            </div>
        );
    }
}

export default ProjectBoard;